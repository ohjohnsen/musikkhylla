{
  "name": "musikkhylla",
  "version": "1.0.0",
  "description": "Visual music rack application with drag-and-drop album management",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && uv run flask --app app run --debug --port 3001",
    "install:all": "npm install && npm install --prefix frontend && cd backend && uv sync",
    "build": "cd frontend && npm run build",
    "start:frontend": "cd frontend && npm run preview",
    "start:backend": "cd backend && uv run flask --app app run --port 3001"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["music", "albums", "visualization", "react", "flask", "python"],
  "author": "",
  "license": "MIT"
}
